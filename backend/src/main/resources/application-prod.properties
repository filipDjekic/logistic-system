# Production must not expose generated API documentation or its UI.
springdoc.api-docs.enabled=false
springdoc.swagger-ui.enabled=false

# Production origins must be supplied explicitly and should use HTTPS.
app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS}
